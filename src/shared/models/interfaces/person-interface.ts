import {IPackage} from './package-interface';

export interface IPerson {
  name: string;
  id: string;
  age: number;
  category: string;
  address: string;
  telephone: string;
  discount: number | boolean;
  screen: string;
  description: string;
}

